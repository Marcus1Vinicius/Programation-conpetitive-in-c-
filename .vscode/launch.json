{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Python",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",  // Usa o arquivo atualmente aberto
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}